# @generated
# =============================================================================
# ⚠️ DO NOT EDIT THIS FILE BY HAND ⚠️
# =============================================================================
# This spin.toml manifest is auto-generated by the Codegen Pipeline.
# Any manual changes will be overwritten on the next build.
#
# SOURCE OF TRUTH:
# To update routes, security policies, or outbound hosts, modify the
# `[package.metadata.service]` section in the respective component's Cargo.toml.
# =============================================================================

spin_manifest_version = 2

[application]
name = "nexus-core"
version = "0.1.0"
authors = ["a.shlyakhto <a.shlyakhto@gmail.com>"]
description = "Nexus - High Performance Core"

[variables]
database_url = { default = "http://localhost:8000" }
database_namespace = { default = "nexus" }
database_name = { default = "core" }
#database_user = { required = true, secret = true }
#database_pass = { required = true, secret = true }
#database_private_key = { required = true, secret = true }
#database_public_key = { required = true, secret = true }
redis_url = { default = "redis://localhost:6379" }
minio_url = { default = "http://localhost:9000" }

[trigger]

[[trigger.http]]
component = "nx-auth"
route = "/api/auth/..."

[[trigger.http]]
component = "nx-audit"
route = "/api/audit/..."

[[trigger.http]]
component = "nx-organization"
route = "/api/org/..."

[[trigger.http]]
component = "nx-account"
route = "/api/account/..."

[[trigger.http]]
component = "nx-access"
route = "/api/access/..."

[component]

[component.nx-auth]
source = "dist/nx-server/components/nx-auth.wasm"
allowed_outbound_hosts = ["{{ database_url }}", "{{ redis_url }}"]
variables = { database_namespace = "{{ database_namespace }}", database_name = "{{ database_name }}", database_url = "{{ database_url }}", redis_url = "{{ redis_url }}" }

[component.nx-audit]
source = "dist/nx-server/components/nx-audit.wasm"
allowed_outbound_hosts = ["{{ database_url }}"]
variables = { database_url = "{{ database_url }}" }

[component.nx-organization]
source = "dist/nx-server/components/nx-organization.wasm"
allowed_outbound_hosts = ["{{ database_url }}"]
variables = { database_url = "{{ database_url }}" }

[component.nx-account]
source = "dist/nx-server/components/nx-account.wasm"
allowed_outbound_hosts = ["{{ database_url }}"]
variables = { database_url = "{{ database_url }}" }

[component.nx-access]
source = "dist/nx-server/components/nx-access.wasm"
allowed_outbound_hosts = ["{{ database_url }}"]
variables = { database_url = "{{ database_url }}" }
